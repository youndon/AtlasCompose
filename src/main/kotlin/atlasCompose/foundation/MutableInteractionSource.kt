package compose.foundation

import androidx.compose.foundation.interaction.MutableInteractionSource

private class MutableInteractionSource {
        val MutableInteractionSource.ss:Any get() = this.apply {
//            this.emit()
//            this.tryEmit()
        }
}