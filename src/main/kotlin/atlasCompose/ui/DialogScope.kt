package atlasCompose.ui

import androidx.compose.desktop.ComposeDialog
import androidx.compose.ui.window.DialogScope

private class DialogScope {
    init {
//        DialogScope(ComposeDialog()).dialog
    }
}