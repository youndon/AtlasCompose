package compose.animation

import androidx.compose.animation.core.AnimationConstants

object AnimationConstants {
    init {
        AnimationConstants.apply {
            DefaultDurationMillis
            UnspecifiedTime
        }
    }
}